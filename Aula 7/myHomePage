import 'package:flutter/material.dart';
import 'package:flutter_application_2/widgets/mycard.dart';
import 'package:flutter_application_2/widgets/mydrawer.dart';
import 'package:flutter_application_2/widgets/mystory.dart';

class Myhomepage extends StatelessWidget {
  const Myhomepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Aula 6',
          style: TextStyle(color: const Color.fromARGB(255, 255, 255, 255)),
        ),
        backgroundColor: Colors.red,
      ),
      drawer: Mydrawer(),
      body: SingleChildScrollView(
        child: Column(
          children: <Widget>[
          SizedBox(
            height: 200,
            child: ListView.builder(
            scrollDirection: Axis.horizontal,
            itemCount: 10,
            itemBuilder: (context, itens) {
             return Mystory(title: "Flutter I");
            },
          ),
        ),
     SizedBox(
            height: 200,
            child: ListView.builder(
            scrollDirection: Axis.horizontal,
            itemCount: 10,
            itemBuilder: (context, itens) {
             return MyCard(title: "Flutter I", image: "", like: false);
            },
          ),
        ),
        
      ],
    ),
  ),
); }
}
